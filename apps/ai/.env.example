# Database (use internal Railway URL in production)
DATABASE_URL=postgresql://collabstudy:collabstudy123@localhost:5432/collabstudy

# Redis (include password if set: redis://:password@host:6379)
REDIS_URL=redis://:collabstudy123@localhost:6379

# OpenAI API key (optional — only needed if using OpenAI-backed features)
OPENAI_API_KEY=your_openai_api_key_here

# Port — Railway sets this automatically; default 8000 locally
PORT=8000
